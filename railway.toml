[build]
builder = "dockerfile"

[deploy]
startCommand = "python -m tasks.scheduler"
restartPolicyMaxRetries = 5
restartPolicyWindow = 60

# Worker process - no timeout
[processes.worker]
maxRestartsPerHour = 5
memoryLimit = "512Mi"
